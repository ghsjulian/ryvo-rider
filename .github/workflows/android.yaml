name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: ryvo-rider/frontend/package-lock.json # Points cache to frontend

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: android-actions/setup-android@v3

      - name: Install dependencies
        working-directory: ryvo-rider/frontend
        run: npm ci

      - name: Build web app
        working-directory: ryvo-rider/frontend
        run: npm run build

      - name: Sync Capacitor
        working-directory: ryvo-rider/frontend
        run: npx cap sync android

      - name: Build APK
        working-directory: ryvo-rider/frontend/android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ryvo-rider-apk
          path: ryvo-rider/frontend/android/app/build/outputs/apk/debug/app-debug.apk
